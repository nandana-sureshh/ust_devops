apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-dynamic
provisioner: cluster.local/dev-nfs-provisioner2-nfs-subdir-external-provisioner
reclaimPolicy: Delete
parameters:
  pathPattern: "${.PVC.namespace}-${.PVC.name}"
  onDelete: delete

  
